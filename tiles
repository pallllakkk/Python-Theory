fl= float(input("Enter the floor length"))
fb= float(input("Enter the floor breadth"))
tl= float(input("Enter the tiles length"))
tb= float(input("Enter the tiles breadth"))
nn= (fl*fb)/(tl*tb)
print("The number of tiles required is =",nn))
